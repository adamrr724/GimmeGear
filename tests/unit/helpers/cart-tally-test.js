import { cartTally } from 'j-crew/helpers/cart-tally';
import { module, test } from 'qunit';

module('Unit | Helper | cart tally');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cartTally([42]);
  assert.ok(result);
});
